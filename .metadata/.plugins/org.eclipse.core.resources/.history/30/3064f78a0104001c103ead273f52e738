package p1;

/*Abstract classes in java:
 * 1) an abstract class can consist of complete and incomplete methods.
 * 2) to create incomplete method it is mandatory to use abstract keyword 
 * 3) an abstract class can be0-100% incomplete.
 * 4) an abstract class can consist of main method.
 * 1)we can keep both complete and incomplete method in it, and we should
 * write abstract keyword mandaterily.
 * 2)even we can write main method.
 * 3) we cannot create and object.
 * 4) we can create static and non static variables.
 * */
public class Ex1 {
	
}
