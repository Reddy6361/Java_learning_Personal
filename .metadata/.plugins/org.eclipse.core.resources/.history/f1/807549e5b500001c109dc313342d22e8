package p1;

/*Abstraction:
 * 1)Interface:
 * a)
 * b)
*/
/*Note : it is like constructing building , ie we should have a blue print 
 * , same way we should develop a blueprint ie which contains all 
 * the features which should be developed in project ie we 
 * create incomplete methods which are nothing but 
 * Features if we don't use that methods it will give 
 * error because class never allows incomplete methods. 
 * */
public interface Ex7 {
	public void test();
}
